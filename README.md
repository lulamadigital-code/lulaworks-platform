# LulaWorks Platform

AI-powered Contractor Operating System — production build.

- `backend/` — Django + DRF API (container-first)
- `mobile/` — Flutter app (parallel track)
- `infra/` — Terraform (AWS) + container config
- `docs/` — architecture & implementation specs

See `docs/DEPLOYMENT.md` for the container-first setup.
